const Point = require('./index');
const p1 = new Point(5, 5);
const p2 = new Point(10, 10);
test('capitalizes the first letter of every word in a sentence', () => {
  expect(Point.distance(p1, p2)).toEqual(7.0710678118654755);
});
